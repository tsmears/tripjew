<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @trip.name %>
</p>

<p>
  <b>Trip Participants:</b>
  <%= @trip.users.map(&:name).join(", ") %>
</p>

<p>
  <b>Total Spent:</b>
  <%= @trip.total_spent %>
</p>

<p>
	<button type ="button" class="btn btn-primary">
  		<%= link_to 'New Expense', new_expense_path %>
	</button>
</p>

<%= link_to 'Edit', edit_trip_path(@trip) %> |
<%= link_to 'Back', trips_path %>
